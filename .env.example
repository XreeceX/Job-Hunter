# Database (PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/jobhunter?schema=public"

# OpenAI (required for AI features)
OPENAI_API_KEY="sk-..."

# Optional: file storage path for local uploads (default: ./uploads)
UPLOAD_DIR=./uploads
